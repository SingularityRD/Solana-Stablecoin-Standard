# =============================================================================
# Docker Ignore File
# =============================================================================
# Reduces build context size and speeds up builds by excluding unnecessary files

# Git
.git
.gitignore
.gitattributes

# Documentation
*.md
docs/
LICENSE
CODE_OF_CONDUCT.md
CONTRIBUTING.md
.github/

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
target/
node_modules/
dist/
build/

# Dependencies and lock files
yarn.lock
package-lock.json
Cargo.lock

# Test files
tests/
trident-tests/
**/tests/
**/test/

# Development files
.env
.env.local
.env.*.local
*.log
*.log.*

# Frontend (not needed for backend build)
example-frontend/

# Scripts (not needed for backend build)
scripts/

# Docker files (avoid recursion)
docker-compose*.yml
Dockerfile*
.docker/

# Admin TUI (terminal app, not needed for backend)
admin-tui/

# CLI (not needed for backend)
cli/

# SDK (TypeScript, not needed for backend)
sdk/

# Temporary files
tmp/
temp/
*.tmp
*.temp

# SSL certificates (should be mounted, not baked in)
ssl/
certs/
*.pem
*.key
*.crt

# Backup files
*.bak
*.backup
*.sql.gz

# OS files
Thumbs.db
Desktop.ini
